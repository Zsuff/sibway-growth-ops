# Release Log — Sibway Logistics

Фіксує факт і результат кожного релізу для трасованості. Приватні дані
та secrets (host, credentials, шляхи, ключі) сюди не потрапляють — лише
дата, середовище, SHA, обсяг змін, результат QA, підтвердження власника
та нейтральні нотатки. Формат узгоджено з `docs/DEPLOYMENT.md` (§13)
репозиторію `Zsuff/sibway-logistics-website`.

| Date | Environment | Commit SHA | Scope | QA result | Owner approval | Notes |
|---|---|---|---|---|---|---|
| 2026-09-19 | production (sibway.com.ua) | `045d1e8` (sibway-website-) | GTM-PVP275H4 integration: replace direct gtag.js with GTM snippet on 38 HTML pages (all uk/en/pl pages + root 404.html; excludes `/index.php` and root `/index.html` stub per owner decision, Variant B); `js/main.js` — first-touch UTM/gclid capture (sessionStorage + 30-day cookie), `generate_lead`/`form_success` dataLayer push after confirmed Formspree success, micro-conversion click tracking (tel/mailto/wa.me/t.me/viber); `contacts.html` (uk/en/pl) — 6 hidden UTM/gclid fields | Local: `node --check` clean, `tidy` structural parity vs `main` on all 38 files, browser-simulated form/UTM/click tests (Phase 3). Production: view-source confirms GTM snippet present + gtag.js absent on sampled pages (uk/index.html, uk/contacts.html); `dataLayer` initialized; one authorized real test submission (test-marked data, test UTM `utm_source=test&utm_medium=test&utm_campaign=phase4_qa&gclid=TESTQA123`) — Formspree independently confirmed receipt with all fields correct; `dataLayer` `form_success` fired exactly once with `service=general, language=uk`; micro-conversion `click_phone` independently confirmed live in GA4 Realtime on a retest. `generate_lead` GA4 Realtime confirmation for the one authorized submission itself is inconclusive — see Notes. | sibwaylogistics@gmail.com — Phase 3 diff approved, Phase 4 release/publish authorized (chat, 2026-09-19) | Deploy performed via SFTP/FileZilla by Claude Code under `computer-use`, on the owner's already-authenticated session, per explicit per-deploy authorization (see updated `docs/DEPLOYMENT.md` §10 in `sibway-logistics-website`). GTM-PVP275H4 published same day as **Version 2** ("v1 — generate_lead, UTM/gclid, microconversions"). Known open item: the one real form submission's resulting `generate_lead` GA4 hit was not independently confirmed in GA4 Realtime — most likely because the browser tab navigated to the GTM/GA4 console shortly after the dataLayer push, before the network beacon completed (the same class of timing issue was reproduced and then fixed for the `click_phone` micro-conversion by waiting before navigating away). The dataLayer push itself, the published container's tag wiring, and the Formspree-side data are all independently verified correct; only the GA4-server-side receipt of that one specific hit is unconfirmed. Pending owner decision: accept as sufficiently verified, or authorize one additional test submission to obtain a clean Realtime confirmation. |
